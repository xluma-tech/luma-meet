# Environment Configuration
NODE_ENV=production

# Server Configuration
PORT=3000
HOSTNAME=0.0.0.0

# CORS Configuration (set to your domain in production)
CORS_ORIGIN=*

# Socket.io URL
# Local: Leave empty (uses window.location.origin automatically)
# Production: Set to your Render URL (e.g., https://luma-meet-abc123.onrender.com)
NEXT_PUBLIC_SOCKET_URL=
